spring:
  datasource:
    url: jdbc:postgresql://${DATABASE_URL:localhost}:${DATABASE_PORT:5429}/${DATABASE_NAME:traker}
    username: ${DATABASE_USERNAME:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver


  docker:
    compose:
      enabled: false

  server:
    address: 0.0.0.0
    port: 8080

  jpa:
    show-sql: ${SHOW_SQL:true}
    properties:
      hibernate:
        default_schema: public
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yml
    default-schema: public

springdoc:
  swagger-ui:
    path: /swagger
# spring.application.name=traker

jwt:
  secret: MzJqOUtMbVBxWHJZeFo4elc1dk4ydFk1dUIzblFlSjljQThkRjZnSDRpTTFw
  access_token_expiration: 86400000 # 24 часа в миллисекундах
  refresh_token_expiration: 604800000 # 7 дней в миллисекундах

auth:
  token_type: Bearer
  scope: api.read api.write

app:
  security:
    master-key: ${APP_MASTER_KEY:Ith717PYYY78K2Tz9Jzb2FbsHGmhQslUwPnyvviBsIY=}

vpn:
  encryptionKey: ${VPN_ENCRYPTION_KEY:0123456789ABCDEF0123456789ABCDEF}
  serverPublicKey: ${VPN_SERVER_PUBLIC_KEY:changeme}
  serverIp: ${VPN_SERVER_IP:127.0.0.1}
  vless:
    server-host: ${VPN_VLESS_SERVER_HOST:144.31.118.242}
    server-port: ${VPN_VLESS_SERVER_PORT:443}
    public-key: ${VPN_VLESS_PUBLIC_KEY:gFnnJuJah8PPRNSPGj82cYvIM7cwqNxgUUsr74NUI0E}
    sni: ${VPN_VLESS_SNI:www.cloudflare.com}
    flow: ${VPN_VLESS_FLOW:xtls-rprx-vision}
    config-path: ${VPN_VLESS_CONFIG_PATH:/usr/local/etc/xray/config.json}
    inbound-tag: ${VPN_VLESS_INBOUND_TAG:vless-reality-in}
    reload-script: ${VPN_VLESS_RELOAD_SCRIPT:/usr/local/bin/xray-reload.sh}
    profile-name-prefix: ${VPN_VLESS_PROFILE_NAME_PREFIX:tracker-vpn}
    dest: ${VPN_VLESS_DEST:www.cloudflare.com:443}
